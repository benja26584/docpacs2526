<%- include('partials/header') %>

    <body>
        <% if (viewport==="online" ) { %>
            <button onclick="window.location='/print'">Print</button>
            <% } %>
    </body>
    <%- include('partials/footer') %>